import { LitElement } from 'lit';
import { BreadcrumbItem } from './hds-breadcrumb-item.js';
export declare type BreadcrumbChangePayload = {
    expanded: boolean;
};
/**
 *
 * @event {CustomEvent<{expanded: boolean}>} change Fired when the breadcrumb status has been expanded `BreadcrumbChangePayload`
 *
 * @slot default List of `hds-breadcrumb-item` elements
 */
export declare class Breadcrumb extends LitElement {
    static styles: import("lit").CSSResultGroup;
    truncateItems: boolean;
    truncateItemBuffer: number;
    protected itemsSlot: HTMLElement;
    protected firstUpdated(): void;
    protected render(): import("lit-html").TemplateResult<1>;
    protected _handleExpand(evt: CustomEvent<string>): Promise<void>;
    protected _setDefaultActive(): void;
    protected _getItems(): BreadcrumbItem[];
}
declare global {
    interface HTMLElementTagNameMap {
        'hds-breadcrumb': Breadcrumb;
    }
}
