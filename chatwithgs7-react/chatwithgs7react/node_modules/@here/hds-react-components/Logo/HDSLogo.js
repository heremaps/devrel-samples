import * as React from 'react';
import { Logo } from '@here/hds-logo';
// eslint-disable-next-line no-new
new Logo();
const HDSLogo = React.forwardRef((props, ref) => {
    const { className, monochrome, size, padding, inverted, background, ...rest } = props;
    return (React.createElement("hds-logo", { ref: ref, size: size, ...(className && { class: className }), ...(inverted && { inverted }), ...(background && { background }), ...(monochrome && { monochrome }), ...(padding && { padding }), ...rest }));
});
HDSLogo.defaultProps = {
    size: 'extra-small',
    inverted: false,
    background: false,
    monochrome: false,
    padding: false,
};
export default HDSLogo;
//# sourceMappingURL=HDSLogo.js.map