import * as React from 'react';
import { LogoSize } from '@here/hds-logo';
interface HDSLogoProperties extends React.HTMLAttributes<HTMLElement> {
    inverted?: boolean;
    monochrome?: boolean;
    size?: LogoSize;
    padding?: boolean;
    background?: boolean;
}
declare const HDSLogo: React.ForwardRefExoticComponent<HDSLogoProperties & React.RefAttributes<unknown>>;
export default HDSLogo;
