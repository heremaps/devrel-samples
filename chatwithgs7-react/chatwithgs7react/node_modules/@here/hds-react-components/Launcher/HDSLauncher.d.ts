import React from 'react';
import { LauncherVariant } from '@here/hds-launcher';
import { HDSPanelProperties } from '../Panel/HDSPanel.js';
interface HDSLauncherProperties extends Omit<HDSPanelProperties, 'title' | 'expanded'> {
    expanded?: boolean;
    variant?: LauncherVariant;
    title?: React.ReactNode;
    subtitle?: React.ReactNode;
    main?: React.ReactNode;
    footer?: React.ReactNode;
    onClose?: () => void;
}
declare const HDSLauncher: React.ForwardRefExoticComponent<HDSLauncherProperties & React.RefAttributes<unknown>>;
export default HDSLauncher;
