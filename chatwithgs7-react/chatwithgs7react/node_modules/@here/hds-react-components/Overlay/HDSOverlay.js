import * as React from 'react';
import { Overlay } from '@here/hds-overlay';
// eslint-disable-next-line no-new
new Overlay();
const HDSOverlay = React.forwardRef((props, ref) => {
    const { children, className, ...rest } = props;
    return (React.createElement("hds-overlay", { ref: ref, ...(className && { class: className }), ...rest }, children));
});
export default HDSOverlay;
//# sourceMappingURL=HDSOverlay.js.map