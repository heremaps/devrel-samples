import React from 'react';
import { HDSModalProperties } from './HDSModal.js';
export interface ModalWrapperProps extends HDSModalProperties {
    modalId?: string;
    body?: React.ReactNode;
}
interface ModalContextType {
    addModal: (modal: ModalWrapperProps) => void;
}
declare const ModalContext: React.Context<ModalContextType>;
export default ModalContext;
export declare const HDSModalContextProvider: React.FC<{
    children: React.ReactNode;
}>;
