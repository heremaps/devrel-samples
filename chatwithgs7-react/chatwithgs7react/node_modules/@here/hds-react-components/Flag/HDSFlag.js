import * as React from 'react';
import { Flag } from '@here/hds-flag';
// eslint-disable-next-line no-new
new Flag();
const HDSFlag = React.forwardRef((props, ref) => {
    const { children, iconCategory, className, ...rest } = props;
    return (React.createElement("hds-flag", { ref: ref, ...(iconCategory && { 'icon-category': iconCategory }), ...(className && { class: className }), ...rest }, children));
});
HDSFlag.defaultProps = {
    variant: 'subtle',
    size: 'small',
    icon: undefined,
    iconCategory: 'core-ui',
};
export default HDSFlag;
//# sourceMappingURL=HDSFlag.js.map