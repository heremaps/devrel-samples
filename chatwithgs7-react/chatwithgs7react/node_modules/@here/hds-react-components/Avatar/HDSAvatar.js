import * as React from 'react';
import { Avatar } from '@here/hds-avatar';
// eslint-disable-next-line no-new
new Avatar();
const HDSAvatar = React.forwardRef((props, ref) => {
    const { disabled, borderless, size = 'medium', name, imageUrl, icon, iconStyle, iconCategory, className, ...rest } = props;
    return (React.createElement("hds-avatar", { ref: ref, ...(disabled && { disabled }), ...(borderless && { borderless }), size: size, name: name, ...(className && { class: className }), ...(imageUrl && { 'image-url': imageUrl }), ...(icon && { icon }), ...(iconStyle && { 'icon-style': iconStyle }), ...(iconCategory && { 'icon-category': iconCategory }), ...rest }));
});
HDSAvatar.defaultProps = {
    disabled: false,
    borderless: false,
    size: 'medium',
    name: '',
    imageUrl: undefined,
    icon: undefined,
    iconCategory: 'core-ui',
    iconStyle: 'solid',
};
export default HDSAvatar;
//# sourceMappingURL=HDSAvatar.js.map