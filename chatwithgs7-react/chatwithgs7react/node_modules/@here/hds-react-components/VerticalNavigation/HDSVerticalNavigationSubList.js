import React from 'react';
import { VerticalNavigationSubList } from '@here/hds-vertical-navigation';
// eslint-disable-next-line no-new
new VerticalNavigationSubList();
const HDSVerticalNavigationSubList = React.forwardRef((props, ref) => {
    const { children, className, ...rest } = props;
    return (React.createElement("hds-vertical-navigation-sub-list", { ref: ref, ...(className && { class: className }), ...rest }, children));
});
export default HDSVerticalNavigationSubList;
//# sourceMappingURL=HDSVerticalNavigationSubList.js.map