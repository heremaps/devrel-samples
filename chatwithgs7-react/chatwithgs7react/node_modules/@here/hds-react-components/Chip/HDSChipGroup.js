import * as React from 'react';
import { ChipGroup } from '@here/hds-chip';
// eslint-disable-next-line no-new
new ChipGroup();
const HDSChipGroup = React.forwardRef((props, ref) => {
    const { children, className, valign, justify, dragging, ...rest } = props;
    return (React.createElement("hds-chip-group", { ...(className && { class: className }), valign: valign, ...(justify && { justify }), ...(dragging && { dragging: true }), ...rest, ref: ref }, children));
});
HDSChipGroup.defaultProps = {
    valign: 'center',
    justify: 'flex-start',
    dragging: false,
};
export default HDSChipGroup;
//# sourceMappingURL=HDSChipGroup.js.map