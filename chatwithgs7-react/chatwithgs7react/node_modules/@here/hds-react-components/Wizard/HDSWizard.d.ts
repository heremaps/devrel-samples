import { StepStatePayload } from '@here/hds-stepper';
import * as React from 'react';
import { HDSModalProperties } from '../Modal/HDSModal.js';
interface HDSWizardProperties extends Omit<HDSModalProperties, 'onThirdButton' | 'titleSize' | 'hideThirdButton'> {
    stepper?: React.ReactNode;
    stepperFooter?: React.ReactNode;
    onStepChange?: (stepState: StepStatePayload) => void;
    stepperHeading?: string;
    stepText?: string;
}
declare const HDSWizard: React.ForwardRefExoticComponent<HDSWizardProperties & React.RefAttributes<unknown>>;
export default HDSWizard;
