import { ProfileMenuChangePayload } from '@here/hds-navbar';
import React, { ReactNode } from 'react';
interface HDSProfileMenuProps extends Omit<React.HTMLAttributes<HTMLInputElement>, 'onChange'> {
    children?: ReactNode;
    profileName?: string;
    profileEmail?: string;
    avatarImageUrl?: string;
    open?: boolean;
    signup?: boolean;
    noTrigger?: boolean;
    onChange?: (e: ProfileMenuChangePayload) => void;
}
declare const HDSProfileMenu: React.ForwardRefExoticComponent<HDSProfileMenuProps & React.RefAttributes<unknown>>;
export default HDSProfileMenu;
