import * as React from 'react';
import { NavbarSideChangePayload } from '@here/hds-navbar';
import { HDSPanelProperties } from '../Panel/HDSPanel.js';
interface HDSNavBarSideProperties extends Omit<HDSPanelProperties, 'expanded' | 'onChange'> {
    collapsed?: boolean;
    title?: string;
    toggleText?: string;
    hideToggle?: Boolean;
    logo?: React.ReactNode;
    main?: React.ReactNode;
    footer?: React.ReactNode;
    onChange?: (e: NavbarSideChangePayload) => void;
}
declare const HDSNavBarSide: React.ForwardRefExoticComponent<HDSNavBarSideProperties & React.RefAttributes<unknown>>;
export default HDSNavBarSide;
