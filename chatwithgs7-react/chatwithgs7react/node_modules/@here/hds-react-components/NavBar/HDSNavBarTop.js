import * as React from 'react';
import { NavBarTop } from '@here/hds-navbar';
// eslint-disable-next-line no-new
new NavBarTop();
const HDSNavBarTop = React.forwardRef((props, ref) => {
    const { children, className, variant, ...rest } = props;
    return (React.createElement("hds-navbar-top", { ref: ref, ...(variant && { variant }), ...(className && { class: className }), ...rest }, children));
});
HDSNavBarTop.defaultProps = {
    variant: 'default',
};
export default HDSNavBarTop;
//# sourceMappingURL=HDSNavBarTop.js.map