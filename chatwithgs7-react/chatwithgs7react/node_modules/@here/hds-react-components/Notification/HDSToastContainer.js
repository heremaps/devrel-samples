import * as React from 'react';
import { ToastContainer } from '@here/hds-notification';
// eslint-disable-next-line no-new
new ToastContainer();
const HDSToastContainer = React.forwardRef((props, ref) => {
    const { children, position = 'bottom-right', className, ...rest } = props;
    return (React.createElement("hds-toast-container", { ...(position && { position }), ...(className && { class: className }), ref: ref, ...rest }, children));
});
HDSToastContainer.defaultProps = {
    position: 'bottom-right',
};
export default HDSToastContainer;
//# sourceMappingURL=HDSToastContainer.js.map