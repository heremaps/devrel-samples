import { NotificationPosition } from '@here/hds-notification';
import React from 'react';
import { HDSNotificationBaseProperties } from './HDSNotification.js';
export interface ToastWrapperProps extends HDSNotificationBaseProperties {
    toastId?: string;
    body?: React.ReactNode;
    id?: string;
    removeToast?: (toastId: string) => void;
}
interface ToastContextType {
    addToast: (toast: ToastWrapperProps) => void;
}
declare const ToastContext: React.Context<ToastContextType>;
export default ToastContext;
export interface HDSToastContextProviderProps {
    position?: NotificationPosition;
    children?: React.ReactNode;
}
export declare const HDSToastContextProvider: {
    ({ position, children }: HDSToastContextProviderProps): JSX.Element;
    defaultProps: {
        position: string;
    };
};
