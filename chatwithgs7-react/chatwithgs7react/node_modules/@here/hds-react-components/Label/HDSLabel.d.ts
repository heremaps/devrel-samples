import * as React from 'react';
import { LabelType, LabelVariant } from '@here/hds-label';
interface HDSLabelProperties extends React.HTMLAttributes<HTMLElement> {
    primaryLabel?: React.ReactNode;
    secondaryLabel?: React.ReactNode;
    type: LabelType;
    variant: LabelVariant;
    for?: string;
}
declare const HDSLabel: React.ForwardRefExoticComponent<HDSLabelProperties & React.RefAttributes<unknown>>;
export default HDSLabel;
