import * as React from 'react';
import { ToggleListItem } from '@here/hds-togglelist';
// eslint-disable-next-line no-new
new ToggleListItem();
const HDSToggleListItem = React.forwardRef((props, ref) => {
    const { children, expanded, className, ...rest } = props;
    return (React.createElement("hds-togglelist-item", { ...(expanded && { expanded }), ...(className && { class: className }), ref: ref, ...rest }, children));
});
HDSToggleListItem.defaultProps = {
    expanded: false,
};
export default HDSToggleListItem;
//# sourceMappingURL=HDSToggleListItem.js.map