import * as React from 'react';
import { Step } from '@here/hds-stepper';
// eslint-disable-next-line no-new
new Step();
const HDSStep = React.forwardRef((props, ref) => {
    const { children, optional, className, label, ...rest } = props;
    return (React.createElement("hds-step", { ...(className && { class: className }), label: label, ...(optional && { optional }), ref: ref, ...rest }, children));
});
HDSStep.defaultProps = {
    optional: false,
    label: '',
};
export default HDSStep;
//# sourceMappingURL=HDSStep.js.map