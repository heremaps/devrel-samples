import React from 'react';
import { TabPanelContainer } from '@here/hds-tabs';
// eslint-disable-next-line no-new
new TabPanelContainer();
const HDSTabPanelContainer = React.forwardRef((props, ref) => {
    const { children, selectedPanel, tabBarId, className, ...rest } = props;
    return (React.createElement("hds-tab-panel-container", { ...(className && { class: className }), ...(tabBarId && { 'tab-bar-id': tabBarId }), ...(selectedPanel && { 'selected-panel': selectedPanel }), ref: ref, ...rest }, children));
});
HDSTabPanelContainer.defaultProps = {
    tabBarId: '',
    selectedPanel: '',
};
export default HDSTabPanelContainer;
//# sourceMappingURL=HDSTabPanelContainer.js.map