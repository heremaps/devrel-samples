import React from 'react';
import { TabPanel } from '@here/hds-tabs';
// eslint-disable-next-line no-new
new TabPanel();
const HDSTabPanel = React.forwardRef(({ show, ariaLabelledby, children, className, ...rest }, ref) => (React.createElement("hds-tab-panel", { ...(className && { class: className }), ...(ariaLabelledby && { 'aria-labelledby': ariaLabelledby }), icon: show, ref: ref, ...rest }, children)));
HDSTabPanel.defaultProps = {
    show: false,
    ariaLabelledby: '',
};
export default HDSTabPanel;
//# sourceMappingURL=HDSTabPanel.js.map