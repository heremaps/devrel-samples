import { LitElement } from 'lit';
import { NotificationActionPayload, NotificationChangePayload, NotificationSize, NotificationStatus, NotificationVariant } from './hds-notification.js';
export declare type NotificationPosition = 'top-left' | 'top-center' | 'top-right' | 'bottom-left' | 'bottom-center' | 'bottom-right';
export declare class ToastContainer extends LitElement {
    static styles: import("lit").CSSResultGroup;
    position: NotificationPosition;
    protected render(): import("lit-html").TemplateResult<1>;
}
export interface ToastProps {
    title: string;
    body?: string;
    bodyCssSelector?: string;
    variant?: NotificationVariant;
    status?: NotificationStatus;
    size?: NotificationSize;
    timeout?: number;
    position?: NotificationPosition;
    onChange?: (e: CustomEvent<NotificationChangePayload>) => void;
    onAction?: (e: CustomEvent<NotificationActionPayload>) => void;
}
export declare const toast: ({ title, body, bodyCssSelector, timeout, position, variant, status, size, onChange, onAction, }: ToastProps) => void;
declare global {
    interface HTMLElementTagNameMap {
        'hds-toast-container': ToastContainer;
    }
}
